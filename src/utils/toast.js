export const successToast = (message = 'Something went wrong') => {
  // can replace with custom toast
  alert(message)
}

export const errorToast = (message = 'Something went wrong') => {
  // can replace with custom toast
  alert(message)
}